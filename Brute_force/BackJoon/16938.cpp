//L <= sum(A) <= R
//max(A) - min(A) >= X
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    int N, L, R, X, n;
    vector<int> A;
    cin >> N >> L >> R >> X;
    for(int i=0; i<N; i++){
        cin >> n;
        A.push_back(n);
    }
    
    

}